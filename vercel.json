{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install && cd clipflow-web && npm install",
  "buildCommand": "cd clipflow-web && npm run build",
  "outputDirectory": "clipflow-web/.next"
}
