{
  "name": "fadeflow",
  "private": true,
  "description": "Monorepo root — Render: Settings → Root Directory = stripe-backend (see render.yaml). Local: npm start cds into stripe-backend.",
  "scripts": {
    "postinstall": "cd stripe-backend && npm install",
    "start": "cd stripe-backend && npm start",
    "build": "cd stripe-backend && npm install"
  }
}
