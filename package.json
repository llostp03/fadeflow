{
  "name": "fadeflow",
  "private": true,
  "description": "Monorepo root — Render can use Root Directory empty + npm install / npm start",
  "scripts": {
    "postinstall": "cd stripe-backend && npm install",
    "start": "cd stripe-backend && npm start",
    "build": "cd stripe-backend && npm install"
  }
}
